# GOLI-CLI Python ML/RL Pipeline dependencies.
#
# Pinned to versions known to work together. Update with care —
# TRL/vLLM/transformers version mismatches are a common source of
# silent training failures.
#
# Python: 3.10+ (3.11 recommended)
# CUDA: 12.1+ (for vLLM GPU inference)
# GPU: 8×H100/H200 recommended for GLM-5.2 744B MoE at FP8

# Core ML framework
torch==2.4.0
transformers==4.44.2
tokenizers==0.19.1
accelerate==0.33.0
safetensors==0.4.5

# Fine-tuning (TRL ≥ 0.12 required for vllm_mode="colocate")
trl==0.12.1
peft==0.12.0  # LoRA adapters
datasets==2.20.0
diffusers==0.30.2  # required by TRL for some sampler paths

# Inference engine (vLLM ≥ 0.6 for co-location)
vllm==0.6.3
bitsandbytes==0.43.3  # 4-bit/8-bit quantization for eval

# Experiment tracking
wandb==0.17.9  # Weights & Biases integration (optional, set WANDB_API_KEY)
tensorboard==2.17.0  # Local fallback

# Eval harness
swebench==2.0.0  # SWE-bench evaluation
datasets==2.20.0  # already above, but listed for clarity

# Utilities
pydantic==2.8.2  # config validation
rich==13.8.0  # pretty CLI output
tqdm==4.66.5
numpy==1.26.4  # pinned below 2.0 for vLLM compat
pandas==2.2.2

# Dev / test
pytest==8.3.2
pytest-cov==5.0.0
ruff==0.6.3  # linter/formatter
mypy==1.11.2  # type checker
