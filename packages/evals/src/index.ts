/**
 * @goli/evals — GOLI-CLI evaluation harness and Golden Set benchmark suite.
 *
 * Phase 12 will populate this package with:
 * - SWE-bench Verified harness (via mini-swe-agent)
 * - Custom domain eval suite
 * - Semantic-error evaluator
 * - Promptfoo red-team config
 * - CI eval gate
 *
 * @module @goli/evals
 */

/**
 *
 */
export const EVALS_VERSION = '0.2.0-phase2';
