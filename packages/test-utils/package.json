{
  "name": "@goli-cli/test-utils",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "echo \"test-utils has no build step (source-only)\" && exit 0",
    "clean": "rm -rf tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}